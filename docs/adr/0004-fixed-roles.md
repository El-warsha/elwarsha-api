# ADR 0004 — Fixed membership roles

## Decision

Use `participant`, `mentor`, `maintainer`, and `admin`, scoped to a
cohort-product engagement.

## Why

The first phase does not need a configurable policy engine. Capabilities are
derived from these roles in one place.
